<link rel="stylesheet" href="../css/create-service.style.css">
<div class="cs-page">

    <div class="cs-page__head">
        <div>
            <div class="section-label">Admin Panel</div>
            <h1 class="cs-title">Delete Service</h1>
        </div>
        <a href="/dashboard" class="cs-back-btn">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
                <path d="M9 11L5 7l4-4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"
                    stroke-linejoin="round" />
            </svg>
            Back to Dashboard
        </a>
    </div>

    {{> alerts}}

    <form action="/api/services/delete/?_method=DELETE" method="POST" class="cs-layout">

        <!-- ── LEFT COLUMN ─────────────────────────── -->
        <div class="cs-main">

            <!-- Basic Info -->
            <div class="cs-card">
                <div class="cs-card__head">
                    <span class="cs-card__dot"></span>Basic Info
                </div>
                <div class="cs-card__body">

                    <div class="field">
                        <label for="title">
                            Service Title <span class="field__required">required</span>
                        </label>
                        <input type="text" id="title" name="serviceName" placeholder="e.g. Full Engine Overhaul" required maxlength="80">
                    </div>
                </div>
            </div>

        </div>

        <!-- ── RIGHT SIDEBAR ───────────────────────── -->
        <div class="cs-sidebar">
            <!-- Publish -->
            <div class="cs-card-delete">
                <div class="cs-card__head">
                    <span class="cs-card__dot delete"></span>DELETE
                </div>
                <div class="cs-card__body">
                    <button type="submit" class="btn btn--primary btn--full btn-delete">
                        Delete Service →
                    </button>
                </div>
            </div>
        </div>

    </form>
</div>

{{!-- <script src="../js/service.js"></script> --}}